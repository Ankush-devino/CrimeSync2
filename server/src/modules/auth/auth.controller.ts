// Team Member 1: Auth Controller

export class AuthController {
  async handleRegister(req: unknown, res: unknown) {
    // Register handler
  }

  async handleLogin(req: unknown, res: unknown) {
    // Login handler
  }

  async handleGetCurrentUser(req: unknown, res: unknown) {
    // Me / Profile handler
  }
}
